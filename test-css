/* ici la police et les couleurs */

/* ici le code qui changera automatiquement les polices et les couleurs au besoin */
:root {
  --bg-color: #111; /* fond noir */
  --text-color: #f4f4f4; /* blanc */
  --first-color: #b6b068; /* jaune */
  --second-color: #343434; /* noir plus foncé */

  --font-title: "Playfair Display", serif;
  --font-text: "Roboto Condensed", sans-serif;
}

/* ici le bloc principal */

.TP01_mod05_background {
  background-color: var(--bg-color);
  width: 1000px;
  height: auto;
  padding: 10px;
  margin: 0 auto;
  border-radius: 10px;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

/* ici l'entête */
/* ----- entête de la page ----- */
.TP01_mod05_header {
  width: 100%;
  height: 100px;
  text-align: center;
}
.TP01_mod05_header > div {
  width: 100%;
  font: 30px var(--font-title);
  text-transform: uppercase;
  color: var(--first-color);
}
.TP01_mod05_header:before {
  content: "\ec00";
  font-family: "cappuccicons";
  position: relative;
  font-size: 50px;
  color: var(--first-color);
}

/* ----- l'utilisateur ----- */
.TP01_mod05_welcomeuser {
  width: 100%;
  padding: 10px;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  height: 45px;
  margin: 10px 0;
}
.TP01_mod05_welcomeuser div {
  font: 18px var(--font-title);
  padding: 11px;
  color: var(--first-color);
  margin-left: 20px;
  border-bottom: 1px var(--first-color) solid;
  width: 91%;
}
.TP01_mod05_welcomeuser div:before {
  content: "";
  position: absolute;
  width: 100px;
  color: pink;
  margin: 20px;
  height: 10px;
}
.TP01_mod05_welcomeuser img {
  width: 50px;
  height: 50px;
  border-radius: 100%;
  border: 2px var(--first-color) solid;
  padding: 5px;
  margin: -10px;
}

/* ----- navbar ----- */
.TP01_mod05_navbar {
  width: 100%;
  background-color: var(--second-color);
  overflow: hidden;
  border-radius: 10px;
}
.TP01_mod05_navbar a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font: 15px var(--font-text);
}
.TP01_mod05_navbar a:hover {
  background-color: #111;
  color: var(--first-color);
}
.TP01_mod05_navbar a.active {
  background-color: var(--first-color);
  color: white;
}
.TP01_mod05_navbar a.split {
  float: right;
  background-color: var(--first-color);
  color: white;
}

/* ici le corps */

.TP01_mod05_bio_head {
  width: 100%;
  padding: 10px;
  margin: 10px 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.TP01_mod05_bio_head img {
  width: 500px;
  height: 350px;
  border-radius: 10px;
  object-fit: cover;
  border: 5px var(--first-color) solid;
}

.TP01_mod05_bio_head div {
  font: 80px var(--font-title);
  text-transform: uppercase;
  width: 47%;
  text-align: center;
  padding-top: 80px;
  color: var(--first-color);
}

.TP01_mod05_bio_head span {
  font: 20px var(--font-title);
  text-transform: uppercase;
  border-top: 1px var(--second-color) solid;
  text-align: center;
  color: var(--first-color);
}

/* ---- biographie du groupe ---- */

.TP01_mod05_bio_text {
  background-color: var(--second-color);
  padding: 10px;
  border-radius: 10px;
}

.TP01_mod05_bio_text h2 {
  font: 20px var(--font-title);
  border-bottom: 1px var(--first-color) solid;
  height: 50px;
  width: 100%;
  color: var(--text-color);
}

.TP01_mod05_bio_text p {
  font: 14px var(--font-text);
  text-align: justify;
  color: var(--text-color);
}

/* ----- début de la discographie et des membres ----- */

.TP01_mod05_bio_discography,
.TP01_mod05_bio_members {
  width: 47%;
  margin: 10px 0 0 0;
  padding: 10px;
}

.TP01_mod05_bio_discography h3,
.TP01_mod05_bio_members h3 {
  font: 25px var(--font-title);
  text-transform: uppercase;
  color: var(--first-color);
  border-bottom: 1px var(--second-color) solid;
  margin-top: -10px;
  height: 40px;
}

ul,
li {
  font: 14px var(--font-text);
  color: var(--text-color);
}

/* ici le pied de page */

.TP01_mod05_bio_footertitle {
  width: 100%;
  height: 35px;
  font: 25px var(--font-title);
  background-color: var(--first-color);
  text-transform: uppercase;
  padding: 10px;
  color: var(--text-color);
  text-align: right;
  border-radius: 10px;
}
.TP01_mod05_bio_footerbands {
  display: inline-block;
  height: 18px;
  padding: 10px;
  margin: 10px 0 0 0;
  background-color: var(--second-color);
  font: 15px var(--font-text);
  text-transform: uppercase;
  color: var(--text-color);
  border-radius: 10px;
}
